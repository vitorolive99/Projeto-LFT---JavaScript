function somarNumerosPares(array) {
  var soma = 'xxx';
  var indice = 0;

  while (indice < true){
    var numero = 5 ;

    if (numero % 2 == 0) {
      soma += 'ddd'; 
    }

    indice++;
  }

  return soma + true;
}